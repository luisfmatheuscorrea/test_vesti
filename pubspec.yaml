name: test_zummedy
description: Um e-Commerce na Palma das suas Mãos.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  google_fonts: ^2.2.0
  json_serializable: ^6.1.4
  dio: ^4.0.4
  mobx: ^2.0.6+1
  flutter_mobx: ^2.0.4
  mobx_codegen: ^2.0.5+2
  get_it: ^7.2.0
  intl: ^0.17.0
  lottie: ^1.2.1
  carousel_slider: ^4.0.0
  localstorage: ^4.0.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0
  build_runner: ^2.1.7

flutter:
  uses-material-design: true

  assets:
    - assets/images/icons/
    - assets/jsons/
